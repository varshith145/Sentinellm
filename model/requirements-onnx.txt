# Export-time / CI-only dependencies for model/export_onnx.py and
# tests/test_onnx_parity.py — not part of gateway/requirements.txt since the
# gateway itself runs the raw PyTorch model, not ONNX.
optimum-onnx>=0.1.0
onnxruntime>=1.28.0
