# Export-time dependency for model/export_onnx.py — not needed at gateway
# runtime. onnxruntime itself lives in gateway/requirements.txt since the
# gateway can run it directly (SENTINELLM_INFERENCE_BACKEND=onnx) and
# tests/test_onnx_parity.py needs it too.
optimum-onnx>=0.1.0
